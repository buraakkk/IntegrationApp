_schema-version: "3.2"
ID: integration
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: integration-destination-content
  type: com.sap.application.content
  requires:
  - name: integration-destination-service
    parameters:
      content-target: true
  - name: integration-repo-host
    parameters:
      service-key:
        name: integration-repo-host-key
  - name: integration-uaa
    parameters:
      service-key:
        name: integration-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: integration_html_repo_host
          ServiceInstanceName: integration-html5-srv
          ServiceKeyName: integration-repo-host-key
          sap.cloud.service: integration
        - Authentication: OAuth2UserTokenExchange
          Name: integration_uaa
          ServiceInstanceName: integration-xsuaa-srv
          ServiceKeyName: integration-uaa-key
          sap.cloud.service: integration
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: integration-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: integration-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - integration.zip
      name: integration
      target-path: resources/
- name: integration
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: integration-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: integration-destination-service
    service-plan: lite
- name: integration-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: integration-xsuaa-srv
    service-plan: application
- name: integration-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: integration-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
